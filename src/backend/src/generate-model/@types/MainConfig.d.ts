/**
 * Interface for the main configuration file.
 * @interface MainConfig
*/
export default interface MainConfig {
    folders: {
        config: string
        profiles: string
        videos: string
    }
}